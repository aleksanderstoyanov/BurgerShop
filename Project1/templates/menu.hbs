{{>header}}
{{>orders}}
       <div id="products-container">
            <h1>Burgers</h1>
           {{#each burgers}}
            <hr>
            <div class="product">
                <section>
                    <div class="info">
                        <h1>{{name}}</h1>
                        <h2>${{price}}</h2>
                       <button type="submit" id="order-button">Order</button>
                    </div>
                </section>
                <img src="{{img}}" alt="">
            </div>
            {{/each}}
            <h1>Drinks</h1>
            {{#each drinks}}
            <hr>
            <div class="product">
                <section>
                    <div class="info">
                        <h1>{{name}}</h1>
                        <h2>${{price}}</h2>
                        <button type="submit" id="order-button">Order</button>
                    </div>
                </section>
                <img src="{{img}}" alt="">
            </div>
             {{/each}}
          </div>
 {{>footer}}       